# Pv1 – HTTP Requests lab

## Doel
HTTP request/response begrijpen en oefenen met de `requests`-module.

## Wat doet dit script?
- Stuurt een **GET** naar `https://httpbin.org/get` met query parameters
- Stuurt een **POST** naar `https://httpbin.org/post` met form data
- Print status code, URL en de JSON die terugkomt

## Hoe uitvoeren?
```bash
python3 pv1_http_requests.py
